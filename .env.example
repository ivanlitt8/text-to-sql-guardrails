# Copia a .env y ajustá si hace falta. No versionar .env.

DUCKDB_PATH=data/vuelos.duckdb
OLLAMA_HOST=http://localhost:11434
GENERATOR_MODEL=sqlcoder
JUDGE_MODEL=qwen2.5:3b
OLLAMA_TIMEOUT=900
DEFAULT_ROW_LIMIT=1000

# Orígenes del frontend (Vite). Lista separada por comas.
FRONTEND_ORIGIN=http://127.0.0.1:5173,http://localhost:5173
